import React, { Component, Fragment } from 'react';
import PropTypes from 'prop-types';



export default class Paging extends Comment {
    render() {
        return (

        )
    }
}
Paging.propTypes = {
    currentPage: PropTypes.currentPage.isRequired,
    totalPages: PropTypes.totalPages.isRequired
}